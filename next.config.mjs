/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "miro.medium.com", "avt.mkklcdnv6temp.com"],
      },
};

export default nextConfig;
